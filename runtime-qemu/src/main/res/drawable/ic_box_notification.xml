<!--
  Box's own mark, as a notification silhouette.

  The shade used to carry `android.R.drawable.stat_sys_warning` — a warning triangle, for a VM that
  was working exactly as intended. This is the same open-box outline as the launcher icon, so the
  ongoing notice is recognisably from the app the user installed.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M30,40 L54,27 L78,40 L54,54 Z M30,40 L30,66 L54,81 L54,54 M78,40 L78,66 L54,81"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="6" />
</vector>
