# The crux of the spike: point AGP at a native aarch64 aapt2 instead of the
# x86-64 one it would otherwise download from Google's Maven repo.
android.aapt2FromMavenOverride=/opt/android-sdk/arm64/aapt2

org.gradle.jvmargs=-Xmx1024m
android.useAndroidX=true
org.gradle.caching=true
